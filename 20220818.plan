Goal:
Create managed VM(s) on quirm as a container for a local OPS Manager installation.

Will most likely be Amazon Linux 2 just because everyone else uses it.

1. installed libvirt libvirt-daemon-config-nwfilter libvirt-daemon-driver-libxl
